<div class="ui segment">
    <div class="ui statistics four">
        <div class="statistic"></div>
        <div class="statistic">
            <div class="text value">
                <%= pageData.firstSeen ? new Date(pageData.firstSeen).toLocaleString('en-GB', { timeZone: 'UTC' }) : 'N/A' %>
            </div>
            <div class="label">
                First Seen
            </div>
        </div>
        <div class="statistic">
            <div class="text value">
                <%= pageData.lastSeen ? new Date(pageData.lastSeen).toLocaleString('en-GB', { timeZone: 'UTC' }) : 'N/A' %>
            </div>
            <div class="label">
                Last Seen
            </div>
        </div>
        <div class="statistic"></div>
    </div>
</div>
